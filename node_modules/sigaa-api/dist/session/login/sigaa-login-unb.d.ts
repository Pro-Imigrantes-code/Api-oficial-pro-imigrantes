import { HTTP } from '../sigaa-http';
import { Session } from '../sigaa-session';
import { Login } from './sigaa-login';
import { UNBPage } from '../page/sigaa-page-unb';
import { SigaaForm } from '../sigaa-page';
/**
 * Responsible for logging in UNB.
 * @category Internal
 */
export declare class SigaaLoginUNB implements Login {
    protected http: HTTP;
    protected session: Session;
    constructor(http: HTTP, session: Session);
    readonly errorInvalidCredentials = "SIGAA: Invalid credentials.";
    protected parseLoginForm(page: UNBPage): SigaaForm;
    /**
     * Current login form.
     */
    protected form?: SigaaForm;
    /**
     * Retuns HTML form
     */
    getLoginForm(): Promise<SigaaForm>;
    /**
     * Start a session on desktop
     * @param username
     * @param password
     */
    protected desktopLogin(username: string, password: string): Promise<UNBPage>;
    /**
     * Start a session on Sigaa, return login reponse page
     * @param username
     * @param password
     */
    login(username: string, password: string, retry?: boolean): Promise<UNBPage>;
    protected parseDesktopLoginResult(page: UNBPage): Promise<UNBPage>;
}
