import { HTTP } from '../sigaa-http';
import { Session } from '../sigaa-session';
import { Login } from './sigaa-login';
import { UFPBPage } from '../page/sigaa-page-ufpb';
import { SigaaForm } from '../sigaa-page';
/**
 * Responsible for logging in UFPB.
 * @category Internal
 */
export declare class SigaaLoginUFPB implements Login {
    protected http: HTTP;
    protected session: Session;
    constructor(http: HTTP, session: Session);
    readonly errorInvalidCredentials = "SIGAA: Invalid credentials.";
    protected parseLoginForm(page: UFPBPage): SigaaForm;
    /**
     * Current login form.
     */
    protected form?: SigaaForm;
    /**
     * Retuns HTML form
     */
    getLoginForm(): Promise<SigaaForm>;
    /**
     * Start a session on desktop
     * @param username
     * @param password
     */
    protected desktopLogin(username: string, password: string): Promise<UFPBPage>;
    /**
     * Start a session on Sigaa, return login reponse page
     * @param username
     * @param password
     */
    login(username: string, password: string, retry?: boolean): Promise<UFPBPage>;
    protected parseDesktopLoginResult(page: UFPBPage): Promise<UFPBPage>;
}
